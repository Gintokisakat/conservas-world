version: '3.8'

services:
  web:
    build: .
    container_name: conservas-world-app
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=8000
    restart: unless-stopped
